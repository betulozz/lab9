# Practice Repository

This repository was created for Chapter 9 lab exercises.

## Author
Betül Özmarangoz

## Purpose
To practice shell commands, Git initialization, status checking, staging, and committing.
## Reflection
Git helps track changes without creating many duplicate filenames.

##
//Reflection Questions
Why is version control better than duplicate files? It eliminates "folder chaos." Instead of having project_v1 and project_final, Git keeps one clean file and allows you to "time travel" to any previous version using snapshots.

Why is git status called a radar or compass? Because it provides immediate orientation. It tells you exactly which files are changed, staged, or untracked so you never get lost in your project.

What is the purpose of the staging area? It’s a "loading dock" where you selectively choose which changes to include in your next snapshot. This lets you group related changes together professionally.

What is the difference between an untracked file and a modified file? An untracked file is brand new to Git; a modified file is one Git already knows but has been changed since the last save.

Why does Git need your name and email? For attribution. Every snapshot (commit) is stamped with your identity so there is a clear record of who made which changes.

What does git init actually create? It creates a hidden .git directory, which is the "brain" of the repository that stores all history and configuration.

Why is it important to write meaningful commit messages? They serve as documentation. A message like "Add todo list" is much more helpful to your future self than a message like "Update".

What was the easiest part of this lab? (Personal answer, e.g., "Initializing the repository with git init was surprisingly simple.")

What was the most confusing part? (Personal answer, e.g., "Understanding the difference between staging and committing at first.")

How does an IDE like VS Code make using Git easier? It provides a built-in terminal and visual cues, like changing the color of file names to show if they are modified (yellow) or untracked (green).//